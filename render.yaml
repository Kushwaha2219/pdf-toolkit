# Render Blueprint — lets you deploy with "New + → Blueprint" instead of
# configuring the service by hand. Render reads this file from the repo root.
services:
  - type: web
    name: pdfforge
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    healthCheckPath: /api/health
    autoDeploy: true
